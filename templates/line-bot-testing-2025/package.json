{
  "name": "line-bot-workshop-template",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest --coverage",
    "test:ui": "vitest --coverage --ui",
    "build": "tsc && tsup --entry.index src/index.ts --clean --minify --format cjs --out-dir api"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@vitest/coverage-v8": "^2.1.4",
    "@vitest/ui": "^2.1.4",
    "prettier": "^3.3.3",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vite-tsconfig-paths": "^5.1.1",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "@line/bot-sdk": "^9.4.4",
    "@hono/node-server": "^1.13.5",
    "hono": "^4.6.9",
    "reflect-metadata": "^0.2.2",
    "tsyringe": "^4.8.0"
  }
}
